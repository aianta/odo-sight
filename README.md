# Odo Sight
TODO

`web-ext run`

# To produce XPI
In VS code run the `compress to XPI` task.

Or if running manually

`Usage: compress_to_xpi.ps1 <path to files to compress> <path where the final XPI should be placed>`
